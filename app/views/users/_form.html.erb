
  <%= form_for(currentUser) do |f| %>
  <% if currentUser.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(currentUser.errors.count, "error") %> prohibited this user from being saved:</h2>

      <ul>
      <% currentUser.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="row">
  <div class="field form-group col-md-4">
    <%= f.label :name %>
    <%= f.text_field :name, class: "form-control" %>
  </div>
  </div>

  <div class="row">
  <div class="field form-group col-md-4">
    <%= f.label :password %>
    <%= f.text_field :password, class: "form-control" %>
  </div>
  </div>

  <div class="row">
  <div class="field form-group col-md-4">
    <%= f.label :email %>
    <%= f.text_field :email, class: "form-control" %>
  </div>
  </div>
  <div class="row">
  <div class="field form-group col-md-4">
    <%= f.label :image %><br>
    <%= f.file_field :image,style: "padding:10px;" %>
  </div>
  </div>

  <div class="actions">
    <%= f.submit "Update",class: "btn btn-default" %>
  </div>
<% end %>
